import React, { useEffect, useState, memo } from "react";
import CardsContainer from "../../components/CardsContainer/CardsContainer";

const HomePage = () => {
    return (
        <div>
            <h1>Sorry, this page doesn't work</h1>
        </div>
    );
}

export default memo(HomePage);